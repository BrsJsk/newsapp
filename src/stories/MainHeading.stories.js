import React from 'react';
import { MainHeading } from '../shared/Text';

export default {
  title: 'Main Heading',
  component: MainHeading,
};

const Template = (args) => <MainHeading> Text</MainHeading>;

export const Primary = Template.bind({});
Primary.args = {};
