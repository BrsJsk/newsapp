import React from 'react';
import { UnderlinedHeading } from '../shared/Text';

export default {
  title: 'Underline Heading',
  component: UnderlinedHeading,
};

const Template = (args) => <UnderlinedHeading> Text</UnderlinedHeading>;

export const Primary = Template.bind({});
Primary.args = {};
